# EasyTPA

Paper & Spigot — Kotlin — teleport requests.

`/tpa`, `/tpaccept`, `/tpdeny`, `/tpasettings`, `/tpawhitelist`.

Per-player settings, auto-accept, whitelist, DB (HikariCP).

```bash
gradle shadowJar
# → build/libs/easytpa-1.0.0.jar
```

Requires PurrCore.
